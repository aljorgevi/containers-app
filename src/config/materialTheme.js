import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
	palette: {
		primary: { main: '#ffda1f' },
		secondary: {
			main: '#fff'
		},
		info: {
			main: '#3a3a3a'
		},
		multilineColor: {
			color: 'red'
		}
	}
});
